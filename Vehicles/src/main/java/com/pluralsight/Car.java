package com.pluralsight;

public class Car extends Vehicle {
    public Car(String color, int numberOfPassengers, int cargoCapacity, int fuelCapacity, int currentSpeed, int acceleration, int brakingPower, int turningRadius, int weight, int maxSpeed) {
        super(color, numberOfPassengers, cargoCapacity, fuelCapacity, currentSpeed, acceleration, brakingPower, turningRadius, weight, maxSpeed);
    }
}
